<?xml version="1.0" encoding="utf-8"?>
<!-- This file provides the main layout for the Safety Checklist screen. -->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFE6A7"
        tools:context=".features.safetyChecklists.ui.SafetyChecklistFragment">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            app:elevation="0dp">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="#99582A"
                app:navigationIcon="@drawable/ic_arrow_back">

                <!--
                This TextView acts as the custom title for the toolbar.
                Using a separate TextView allows for precise alignment, like centering the title.
                -->
                <TextView
                    android:id="@+id/sc_toolbar"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="Safety Checklist"
                    android:contentDescription="@string/dashboard_checklist_title"
                    android:textAppearance="@style/ToolbarTitleText" />

            </com.google.android.material.appbar.MaterialToolbar>
        </com.google.android.material.appbar.AppBarLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/safety_checklist_recycler_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_above="@+id/save_button"
                android:clipToPadding="false"
                android:paddingBottom="8dp"
                tools:listitem="@layout/item_safety_checklist" />

            <!--
             This MaterialButton is styled directly in the layout.
             Using app:backgroundTint and app:cornerRadius is the correct way
             to style Material buttons, avoiding conflicts with custom drawables.
            -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/save_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:layout_centerHorizontal="true"
                android:layout_marginBottom="16dp"
                android:paddingStart="32dp"
                android:paddingEnd="32dp"
                android:text="Save List"
                android:contentDescription="@string/checklist_save_button"
                android:textColor="#ffe6a7"
                app:backgroundTint="#6f1d1b"
                app:cornerRadius="16dp" />

        </RelativeLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>